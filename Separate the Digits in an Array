class Solution:
    def separateDigits(self, nums: list[int]) -> list[int]:
        res = []
        for num in nums:
            for ch in str(num):
                res.append(int(ch))
        return res
